[package]
name = "program-revert"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
risc0-zkvm = { version = "=3.0.5", default-features = false }
serde = "1.0"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
