[package]
name = "token-mint-authority-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
token_mint_authority_core = { path = "../core" }

[package.metadata.risc0]
methods = ["guest"]
